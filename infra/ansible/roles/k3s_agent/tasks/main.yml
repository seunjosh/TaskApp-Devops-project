---
- name: Install k3s agent
  shell: |
    curl -sfL https://get.k3s.io | K3S_URL=https://{{ hostvars['control-plane']['private_ip'] }}:6443 K3S_TOKEN={{ hostvars['control-plane']['k3s_token'] }} sh -
  args:
    creates: /usr/local/bin/k3s-agent
  become: yes
