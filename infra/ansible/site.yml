---
- name: Prepare all nodes
  hosts: all
  roles:
    - common

- name: Install k3s control plane
  hosts: k3s_server
  roles:
    - k3s_server

- name: Install k3s worker nodes
  hosts: k3s_agents
  roles:
    - k3s_agent
